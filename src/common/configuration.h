/* IP address where the server is running. This is used by the client to establish a connection. */
#define SERVER_LISTEN_HOST "127.0.0.1"
/* TCP port where the server will be listening for new connections. */
#define SERVER_LISTEN_PORT 61623

/* Location relative to the CWD where the SQLite file will be. */
#define SERVER_DB_LOCATION "catalog.db"
